# packages

Shared building blocks for `liberte-top` repositories.

Current scope is intentionally narrow:

- `npm/auth`: minimal front-end auth runtime for scope checks and auth-context refresh.
- `npm/components`: internal Svelte component library for local web projects.

This repository is meant for stable shared primitives, not product code.
